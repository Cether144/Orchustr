[pytest]
addopts = -p no:cacheprovider
cache_dir = tests/.pytest_cache
; Skip orphaned sandbox cache directories that are permission-locked on
; Windows; without this, collection aborts with PermissionError.
norecursedirs = pytest-cache-files-* .* build dist node_modules
