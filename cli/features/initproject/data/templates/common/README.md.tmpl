# {{project_name}}

Generated by `orchustr init`.

- Language: {{language}}
- Topology: {{topology}}
- Provider: {{provider}}

## Getting started

1. Copy `.env.example` to `.env` and set `{{provider_env}}`.
2. Check the graph: `orchustr lint .`
3. Run the agent: `orchustr run .`

The agent graph lives in `graph.yaml`; project settings live in `orchustr.yaml`.
Add nodes with `orchustr new node <name>`.

Docs: {{repo_url}}/tree/main/docs
